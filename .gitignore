.env
.DS_Store
.idea/
__pycache__/
.venv/
dist/

# Claude/tool meta files
.taskmaster/
memory/
.mcp.json
site/.wrangler/

# PRD (local planning doc only)
PRD.md

# Local research / scratch notes
evidence-registries.md
architecture.html

# Local archive (one-off scripts + enrichment data, never push).
# All one-off/bot scripts (including any that touch Wikidata credentials)
# belong in archive/scripts/, not scripts/, so this single rule keeps them
# out of git without needing per-filename patterns here.
archive/
